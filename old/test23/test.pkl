class Builder {
	executable: String
	flags: 		String
	sources: 	Listing<String>
	outputs: 	Listing<String>
//	function getInstructions(): returnType =
}

local objs = new Builder {
	executable = "CXX"
	flags = ""
	sources =  new { "**/*.cppm" "test.cpp" }
	outputs = sources.toList().map((source) -> source + ".o").toListing()
}

local pp = read*("**/*.pkl").toMap().keys.toList()//.toDynamic()


instructions = new {
	...objs.outputs
	pp
}

function preprocess(a, b) = a

//new Builder {"","",{},{}}
