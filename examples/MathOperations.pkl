amends "@build-system/Manifest.pkl"
import "@c++/Executable.pkl" as CppExecutable
import "@utils/os.pkl" as OS

targets {
	new CppExecutable {
		name = "./MathOps"
		standard = "c++23"
		debug = true
		modules {
			... CppExecutable.findModules(".", "*.cppm")
		}
		compiledModulePath = (modulePath) ->
			outputDir + OS.fileName(modulePath) + ".o"
		sources {

		}
		libraries {
		}
	}.target()
}
