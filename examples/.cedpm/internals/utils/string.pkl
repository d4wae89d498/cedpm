function _join(lst: Listing<String>, idx: Number): String =
	if (idx > lst.length - 1)
		""
	else if (idx == lst.length - 1)
		lst[lst.length - 1]
	else
		lst[idx] + " " + _join(lst, idx + 1)


function join(lst: Listing<String>) = _join(lst, 0)
